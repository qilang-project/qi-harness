// 并行 — 查询级并行 LLM 扇出
//
// Agent 常要同时问 N 个独立子问题（并行总结 5 篇文档 / 并行问 3 个角度），
// LLM 每次几秒，串行是纯浪费。本模块提供并行 LLM 调用：每个提示各开一个
// **独立会话**并发调模型，结果按输入下标对齐返回。
//
// 会话隔离：LLM 会话带历史状态，多路共享一个会话会串话/竞态。
// 所以每个提示用同一份 端点/密钥/模型 配置**新建**自己的会话，问完即关。
//
// 并发骨架 —— 「先全发、后按序收」（fire-then-await）：
//   1. 发射循环：对每个提示 开会话 → 底层模型.异步对话 拿 未来<字符串>
//      （runtime 在后台线程发 HTTP，N 路立即同时在飞），future 存指针列表。
//   2. 收割循环：按 0..个数 顺序 等待 各 future → 结果直接按输入下标 append，
//      对齐天然成立；问完关那一路的会话。
//   任一路失败不崩：开会话失败该槽放错误串；等待 失败 future 会抛 Qi 异常，
//   用 尝试/捕获 接住转错误串。
//
// 为什么不照 工具.qi 并发派发 的 goroutine+通道 骨架？两个 runtime 层的坑：
//   a) 同步 对话（qi_llm_chat）在整个 HTTP 请求期间持有全局会话池 Mutex ——
//      跨会话的同步 对话 被 runtime 串行化，goroutine 再多也白搭
//      （实测 3 提示：并行 26.7s ≈ 串行 29.4s）。异步对话（qi_llm_chat_async）
//      是「克隆会话 → 放锁 → 后台线程发 HTTP → 回锁补历史」，真并行。
//   b) goroutine 里 等待 失败的 future 无 尝试 frame 时转 panic 进协程异常
//      队列，goroutine 静默死掉、完成信号永远不来 → 主线程死等（实测挂死）。
//   fire-then-await 在主流程 等待，异常可捕获，也不再需要通道/编码表。

包 Harness;

导入 标准库.字符串;
导入 标准库.列表 作为 列表库;
导入 标准库.大模型 作为 底层模型;
导入 Harness.模型::{模型配置, 大模型, 开启会话, 关闭会话Harness};

// 并行对话_配置 —— 用已配好的 模型配置（Harness.模型）并行问 N 个提示。
//   提示列表：标准库.列表 字符串列表句柄
//   返回：新的结果字符串列表句柄，结果[i] 对应 提示[i]
公开 函数 并行对话_配置(配置: 模型配置, 提示列表: 整数, 个数: 整数) : 整数 {
    // 发射：每路独立会话 + 异步对话，futures 立即同时在飞。
    // 会话表按输入下标对齐（开失败记 0）；期货表只存成功路的 future，
    // 收割时用游标对应 —— 指针列表没有可对齐的空占位。
    变量 会话表: 整数 = 列表库::创建整数列表();
    变量 期货表: 整数 = 列表库::创建指针列表();
    变量 i: 整数 = 0;
    当 (i < 个数) {
        变量 提示: 字符串 = 列表库::获取字符串(提示列表, i);
        变量 句柄: 整数 = 开启会话(配置);
        列表库::添加整数(会话表, 句柄);
        如果 (句柄 > 0) {
            变量 未来回复: 未来<字符串> = 底层模型.异步对话(句柄, 提示);
            列表库::添加指针(期货表, 未来回复);
        }
        i = i + 1;
    }

    // 收割：按输入顺序 等待，结果直接对齐 append；每路问完即关会话
    变量 结果列表: 整数 = 列表库::创建字符串列表();
    变量 游标: 整数 = 0;
    变量 j: 整数 = 0;
    当 (j < 个数) {
        变量 该路句柄: 整数 = 列表库::获取整数(会话表, j);
        如果 (该路句柄 > 0) {
            变量 文本: 字符串 = "";
            尝试 {
                变量 该路未来: 未来<字符串> = 列表库::获取指针(期货表, 游标);
                文本 = 等待 该路未来;
            } 捕获 错误 {
                文本 = "[并行] 错误：" + 错误;
            }
            游标 = 游标 + 1;
            关闭会话Harness(该路句柄);
            列表库::添加字符串(结果列表, 文本);
        } 否则 {
            列表库::添加字符串(结果列表, "[并行] 错误：会话开启失败");
        }
        j = j + 1;
    }
    返回 结果列表;
}

// 并行对话 —— 散参数便利版：端点/密钥/模型 一行进来，内部走 并行对话_配置。
公开 函数 并行对话(端点: 字符串, 密钥: 字符串, 模型: 字符串, 提示列表: 整数, 个数: 整数) : 整数 {
    变量 配置: 模型配置 = 大模型(端点, 密钥, 模型);
    返回 并行对话_配置(配置, 提示列表, 个数);
}

// 并行映射 —— 对每个项目用模板生成提示再并行问。
//   模板含一个 `{}` 占位，如 "用一句话介绍{}"；用 字符串::替换 换成项目文本。
//   项目列表：字符串列表句柄；返回对齐下标的结果列表句柄。
公开 函数 并行映射(端点: 字符串, 密钥: 字符串, 模型: 字符串, 模板: 字符串, 项目列表: 整数, 个数: 整数) : 整数 {
    变量 提示列表: 整数 = 列表库::创建字符串列表();
    变量 i: 整数 = 0;
    当 (i < 个数) {
        变量 项目: 字符串 = 列表库::获取字符串(项目列表, i);
        列表库::添加字符串(提示列表, 字符串::替换(模板, "{}", 项目));
        i = i + 1;
    }
    返回 并行对话(端点, 密钥, 模型, 提示列表, 个数);
}
