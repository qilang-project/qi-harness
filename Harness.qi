// qi-harness — LLM agent 框架
//
// 基于 Martin Fowler "harness engineering" 模式（martinfowler.com/articles/harness-engineering.html）
// 把 LLM 调用包在可观测 + 可重试 + 可评估的外壳里，让 agent 像普通服务一样
// 可调试、可测试、可监控。
//
// 核心 = 底层 大模型 FFI 已提供「调一次模型」；harness 层提供：
//   - Agent loop：自动 tool dispatch，不用手写「调 → 看 → 执行 tool → 再调」
//   - 追踪：每次调用、每次 tool 执行都打 event
//   - 重试：模型 429 / 网络抖动自动 backoff
//   - 评估：一组测试用例 + 期望，跑出来跟期望比对，agent 改了立刻看影响
//   - 循环（loop engineering）：把「手写 prompt」上移到「设计驱动 agent 的系统」——
//     目标驱动的递归循环 制造者→校验者→反馈→再来，直到完成/升级/超限
//   - 记忆：SQLite 长期记忆（事实/经验/偏好），回忆注入提示词 + 收官提炼经验，
//     跨进程持久 —— agent 记得用户偏好和上次任务的打法

包 Harness;

公开 导入 模型;
公开 导入 对话;
公开 导入 工具;
公开 导入 受控工具;
公开 导入 代理;
公开 导入 追踪;
公开 导入 事件;
公开 导入 重试;
公开 导入 技能;
公开 导入 MCP客户端;
公开 导入 运行上下文;
公开 导入 工具上下文;
公开 导入 会话存储;
公开 导入 运行日志;
公开 导入 可靠流;
公开 导入 安全上下文;
// 评估 / 循环 / 文件工具 / 记忆 / 图 / 向量记忆 / 上下文 模块单独 import —
// 跨包 struct 解析当前有限制，主入口不 re-export
// MCP 服务端（把 Harness 工具暴露成 MCP server；依赖新增 runtime FFI，需重建 qi）：导入 Harness.MCP服务::{MCP服务, 创建MCP服务, 服务注册工具, 运行MCP服务, 关闭MCP服务};
// 评估：导入 Harness.评估::{评估套件, 创建套件, 添加测例, 添加测例带方式, 添加裁判测例, 运行套件, 从JSON加载, 保存基线, 对比基线};
// 循环：导入 Harness.循环::{运行循环, 带记忆运行循环, 循环结果, 循环结果转文本};
// 文件工具：导入 Harness.文件工具::{文件沙箱, 创建文件沙箱, 允许写, 允许删除, 设置读上限, 装备文件工具};
// 记忆：导入 Harness.记忆::{打开记忆, 记住, 回忆, 遗忘, 记忆条数, 关闭记忆};
//       （代理集成：Harness::{启用记忆, 带记忆运行, 提炼经验} 经 代理 模块 re-export）
// 图（图式控制流 / LangGraph-lite + 断点续跑 + HITL）：导入 Harness.图::{图, 创建图, 添加节点, 添加边, 添加条件边, 运行图, 运行图带检查点, 恢复图, 是中断, 中断问题, 是完成, 终点};
// 预算（token/成本硬上限）：导入 Harness.预算::{创建预算, 记入, 记入成本, 超预算, 令牌余量, 预算报告};
// 多代理（团队 + handoff）：导入 Harness.多代理::{团队, 创建团队, 添加成员, 运行团队};
// 递归分解（Hierarchical 分解-委派-汇总，带最大深度护栏）：导入 Harness.递归分解::{递归求解};
// 目标监控（Goal Setting & Monitoring；确定性进度 + 可选 LLM 评估）：导入 Harness.目标监控::{目标, 创建目标, 添加里程碑, 标记完成, 进度, 是否达成, 目标报告, 评估进度};
// 优先级（Prioritization 任务优先队列；确定性调度 + 可选 LLM 打分）：导入 Harness.优先级::{优先队列, 创建队列, 入队, 出队, 队列大小, 队列报告, 评优先级};
// 工具白名单（每代理精细权限治理）随 代理 模块自动可用：设置工具白名单(代理, "工具A,工具B") + 工具是否放行(代理, 名)
// 护栏（注入检测/PII脱敏/输出守卫/工具参数策略）：导入 Harness.护栏::{检测注入, 脱敏, 校验输出, 工具参数放行, 守卫运行};
// 可观测 OTLP 导出（经 追踪 模块）：导入 Harness.追踪::{设置OTLP, 新追踪, 开始跨度, 结束跨度};
// 生命周期事件（进程内同步总线）：导入 Harness.事件::{新运行ID, 订阅生命周期事件, 清空生命周期订阅者, 发出生命周期事件};
// 向量记忆（语义检索）：导入 Harness.向量记忆::{打开向量库, 语义记住, 语义回忆, 余弦相似度, 关闭向量库};
// 上下文（窗口管理）：导入 Harness.上下文::{估算令牌, 历史令牌估算, 需要压缩, 滑窗裁剪, 摘要压缩};
