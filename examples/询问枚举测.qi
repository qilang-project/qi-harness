// 询问::<T> 枚举字段 —— 编译期把枚举变体名注入 schema 约束，模型只能输出合法变体，
// 反序列化(枚举序号)映射回强类型枚举。中文枚举 + LLM。
包 主程序;
导入 标准库.输入输出 作为 IO;
导入 标准库.操作系统 作为 系统;
导入 标准库.大模型 作为 大模型;

枚举 情绪 { 开心, 难过, 生气, 平静 }
类型 分析 { 主题: 字符串, 情绪: 情绪 }

函数 情绪名(e: 情绪) : 字符串 {
    匹配 e {
        开心 => { 返回 "开心"; }
        难过 => { 返回 "难过"; }
        生气 => { 返回 "生气"; }
        平静 => { 返回 "平静"; }
    }
}

函数 入口() {
    变量 会话: 整数 = 大模型.创建会话(系统.获取环境变量("QI_CHAT_URL"), 系统.获取环境变量("QI_CHAT_MODEL"), 系统.获取环境变量("QI_CHAT_KEY"));
    变量 r: 分析 = 询问::<分析>(会话, "分析这句话的情绪，主题一个词：我今天升职加薪太开心了！");
    IO.打印行("主题: " + r.主题);
    IO.打印行("情绪: " + 情绪名(r.情绪));
    大模型.关闭会话(会话);
}
