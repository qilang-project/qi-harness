// 一等流迭代：对于 片段 在 流式(会话, 提示) —— token 流当原生 for-in 迭代，自动关流。
包 主程序;
导入 标准库.输入输出 作为 IO;
导入 标准库.操作系统 作为 系统;
导入 标准库.大模型 作为 大模型;

函数 入口() {
    变量 会话: 整数 = 大模型.创建会话(系统.获取环境变量("QI_CHAT_URL"), 系统.获取环境变量("QI_CHAT_MODEL"), 系统.获取环境变量("QI_CHAT_KEY"));
    IO.打印行("流式输出：");
    对于 片段 在 流式(会话, "用一句话介绍北京") {
        IO.打印(片段);
    }
    IO.打印行("");
    IO.打印行("[流结束]");
    大模型.关闭会话(会话);
}
